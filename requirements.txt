# Core orchestration + agent stack
temporalio==1.26.0
langgraph==1.1.10
langchain-ollama==1.1.0
langchain-core==1.3.2

# Data + external API
pandas==2.3.3
httpx==0.28.1
matplotlib==3.10.9

# Web chat UI
fastapi==0.136.1
uvicorn==0.46.0

# Optional: cloud LLM path (only needed if GROQ_API_KEY is set; the demo runs
# fully local on Ollama without it).
groq==1.2.0
